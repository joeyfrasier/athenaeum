# Database Configuration
#
# Option 1: TigerData Cloud (Recommended for Production)
# Sign up at https://www.tigerdata.com/mst-signup (30-day free trial)
# Get connection string from Tiger Console → Your Service → Connection Info
# DATABASE_URL=postgres://tsdbadmin:YOUR_PASSWORD@YOUR_HOST.tsdb.cloud.timescale.com:PORT/tsdb?sslmode=require
#
# Option 2: Local PostgreSQL/TimescaleDB (Development)
DATABASE_URL=postgresql://tsdbadmin:password@localhost:5432/tsdb
#
PGHOST=localhost
PGPORT=5432
PGDATABASE=tsdb
PGUSER=tsdbadmin
PGPASSWORD=password
DB_SCHEMA=public

# Slack Configuration
SLACK_APP_TOKEN=xapp-your-app-token-here
SLACK_BOT_TOKEN=xoxb-your-bot-token-here

# AI/LLM Configuration
ANTHROPIC_API_KEY=sk-ant-your-api-key-here
OPENAI_API_KEY=sk-your-openai-key-here

# GitHub Configuration
GITHUB_TOKEN=ghp_your-github-token-here
GITHUB_ORG=your-org-name

# Agent Configuration
WORKER_POOL_SIZE=5
EVENT_VISIBILITY_TIMEOUT=300
MAX_RETRY_COUNT=3
COMPANY_NAME=Your Company
DOMAIN_AREAS=Python,Databases,APIs

# MCP Server Configuration
#
# TigerData Hosted MCP Server (Recommended)
# Public endpoint for PostgreSQL/TimescaleDB documentation search
DOCS_MCP_URL=https://mcp.tigerdata.com/docs
DOCS_MCP_TRANSPORT=http
#
# Local MCP Servers (if self-hosting)
SLACK_MCP_URL=http://localhost:3001/mcp
# DOCS_MCP_URL=http://localhost:3002/mcp
GITHUB_MCP_URL=stdio
#
# Tiger CLI MCP (Install via: tiger mcp install claude-code)
TIGER_CLI_MCP=false

# Observability (optional)
LOGFIRE_TOKEN=
LOGFIRE_PROJECT_NAME=

# Environment
ENVIRONMENT=development
LOG_LEVEL=INFO
